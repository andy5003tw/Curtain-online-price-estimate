# 價格後台：公式設定與新增試算產品

## 目標與範圍

價格後台的每個產品都有「公式設定」入口，可分別調整現有 8 種計算模板的商業參數。後台也可建立新的試算產品，先存草稿並試算，確認後才上架到 `/calculator/`。新產品只出現在試算器，不自動建立 `/products/` 介紹頁或 SEO 頁。

本功能不變更「總安裝費最低以 1300 計算」這段既有文案，也不新增 1300 元下限；文案與現行 PHP 算法的差異另案處理。

## 管理操作

1. 登入 `/admin/pricing/`。原有價格卡欄位完整保留：owner、admin、editor 都可編輯單價、軌道最低尺數、每才最低計價與每才基本安裝費。owner、admin 另可按產品旁的「公式設定」或頁面頂部的「新增試算產品」；editor 可查看公式頁，但進階公式參數唯讀。
2. 公式設定頁列出計算步驟、實際生效的倍率、加放量、起算數與取整方式。輸入自訂尺寸後按「預覽」，可同時比較 150 × 150、200 × 240 和自訂尺寸的材料費、安裝費與合計。
3. 預覽後才能儲存。若預覽期間其他人改了規則，須重新載入與預覽；儲存會備份整份規則並留下稽核紀錄。
4. 新產品須選一個既有公式模板，填中文名稱、各項單價與參數，預覽後建立草稿。系統自動分配不重用的 `Pnnn` ID。
5. 草稿在公式頁再次預覽後可上架。上架後產品清單及計算 API 立即從正式站的 PHP 規則讀取；下架會保留設定，但前台不能選擇或試算。

## 原有欄位與公式設定的設計

價格卡延續原有後台的欄位與順序，並保留規則檔中既有的其他價格欄位。新增的「公式設定」是另一個入口，不取代價格卡。權限分工如下：

| 操作 | owner | admin | editor |
| --- | --- | --- | --- |
| 編輯價格卡的原有單價與起算欄位 | 可 | 可 | 可 |
| 查看逐產品公式設定 | 可 | 可 | 可（唯讀） |
| 預覽、儲存進階公式參數；新增與上架產品 | 可 | 可 | 不可 |
| 管理人員、回滾規則 | 可 | 不可 | 不可 |

| 產品公式類型 | 原有價格卡的起算欄位 | 對應的 `formula_settings` 欄位 |
| --- | --- | --- |
| `standard_track`、`seamless_sheer`、`snake_fold`、`roman_shade`、`hospital_curtain` | 軌道最低尺數 `pricing.min_track_ft` | `min_track_ft` |
| `roller_blind`、`area_based`、`vertical_blind` | 每才最低計價 `pricing.min_per_tai` | `min_per_tai` |
| `roller_blind`、`area_based`、`vertical_blind` | 每才基本安裝費 `pricing.base_installation_per_tai` | `min_install_tai`（計算時作為安裝最低計價才數） |

顯示價格卡時，起算欄位取**目前試算實際使用的值**：已儲存的 `formula_settings` 優先，否則使用舊規則的有效值。P002／P012 尚未明確設定時依舊使用 5 尺；在價格卡改動後就以新值試算。價格卡儲存起算欄位時，同時更新 `pricing` 與對應的 `formula_settings`；公式頁儲存時也會把這三項同步回 `pricing`，避免兩個入口顯示不同值或編輯者的修改被舊設定蓋過。單價只在價格卡管理。

價格卡的儲存沿用原有 CSRF、備份與稽核紀錄，並以規則檔版本檢查和鎖定避免同時儲存互相覆蓋；公式頁儲存另須先完成預覽。價格卡維持原有小數輸入格式，公式頁的起算數則要求整數；若價格卡填入小數，之後在公式頁儲存完整公式前須先把該起算數調為整數。

## 資料與接口

正式站目前回報 PHP 7.1.33（2026-09-24 驗證）；後台與 API 的新增程式須使用 PHP 7.1 可執行的語法。

- `private/pricing-rules.php` 仍是正式站報價規則的唯一資料來源。每個產品可新增 `formula_settings` 與 `status`；舊產品缺少 `status` 時視為已上架，缺少 `formula_settings` 時沿用舊公式的有效值。
- `formula_settings` 只包含所選模板允許的數值與 `ceil`、`nearest`、`floor` 取整選項。1 尺＝30.3 cm、1 碼＝3 尺等單位換算固定。P002／P012 原本的 `min_track_ft` 雖存在於資料，舊引擎實際固定用 5 尺；升級後在尚未明確儲存新設定前仍維持 5 尺。
- 價格卡上的原有起算欄位顯示實際生效值。從價格卡修改時，同步寫入 `pricing` 與對應的 `formula_settings`，儲存後試算 API 立即使用新值；從公式頁修改時也同步回價格卡。這讓 editor 可繼續調整原有欄位，同時保留進階公式設定的 owner/admin 權限。
- `GET /api/products.php` 回傳已上架產品的 `id`、`name`、`requires_track`；回應不快取。`/calculator/` 從這支 API 更新選單與「所有可估價品項」。靜態建置中的既有產品清單只供初始畫面與靜態展示使用。
- `POST /api/calc.php` 的請求與成功回應欄位維持原樣；草稿及下架產品回傳與不存在產品相同的 404。
- `private/runtime/product-sequence.json` 保留已分配的最高產品編號，與規則備份分開，因此回滾價格規則後也不重用產品 ID。

## 驗收與部署

- 驗證 13 項既有產品在常用尺寸及起算邊界的結果與升級前一致，並驗證預覽結果等於正式計算 API。
- 驗證 owner/admin/editor 權限、CSRF、數值驗證、並行儲存衝突，以及「草稿 → 上架 → 下架 → 重新上架」和直接帶入 `?product=Pnnn`。
- 執行 PHP 語法與計算回歸測試、`pwsh -NoLogo -ExecutionPolicy Bypass -File scripts/test-pricing-workflow.ps1`、`npm.cmd run build`、`npm.cmd run seo:check`。流程測試在系統暫存目錄建立隔離網站，不修改正式規則。PHP 檔需要另外部署；現有 `deploy:ftp` 只部署靜態 `out/`，不能用它上傳後台或 API。
- 部署時先備份正式站的 `private/pricing-rules.php`，上傳程式碼但保留正式站這份可變動資料及帳號檔。上線後以實際登入、產品清單 API 和試算 API 驗收。

## 2026-09-24 上線紀錄

- 正式站規則先備份到本機忽略檔 `private/backups/live-predeploy-20260924-182017.php`；部署前後 SHA256 均為 `18C84976101770C92BB73653667EE3409A75E0DA6F73BFE55EA82AF3D8D6EBB8`。線上原有 13 項產品中有 12 項與儲存庫範例價格不同，因此未上傳或覆蓋規則檔。
- PHP 後台、API 及共用程式已上傳。主機實際為 PHP 7.1.33；初次部署後發現新版語法不相容，已改寫並重新上傳，計算 API 恢復 HTTP 200。
- 首頁、試算頁與建置資產以精準路徑上傳 32 檔，成功 32、失敗 0。正式站首頁及 `/calculator/` 均為 HTTP 200，產品清單 API 回傳 13 項；13 項現有產品的線上試算金額均與備份規則及新版引擎相符。
- 新產品完整生命週期、權限、CSRF、公式變更及過期預覽在隔離測試站通過。正式後台需要真實管理員登入，因此尚未在正式站建立測試產品或操作管理表單。

## 2026-09-24 原有欄位恢復

- 價格卡恢復上線前既有的軌道最低尺數、每才最低計價、每才基本安裝費及原有價格欄位；owner、admin、editor 均可在原頁調整。進階公式參數仍由 owner、admin 在「公式設定」頁管理。
- 價格卡的起算欄位顯示實際生效值；從價格卡儲存時同步至 `formula_settings`，避免先前已儲存的公式設定蓋過 editor 的修改。隔離測試確認 P001、P002、P005 修改後，公式頁與計算 API 使用相同新值。
- 本次只上傳 `admin/pricing/index.php`，FTP 回報 uploaded=1、failed=0；未上傳正式站的 `private/pricing-rules.php`。重新載入正式後台後，P001／P002／P012 可見「軌道最低尺數」，P005 等面積產品可見兩個原有起算欄位，原有規則版本仍為 `manual-20260924-173238`。
